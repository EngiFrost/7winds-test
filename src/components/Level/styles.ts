import styled from '@emotion/styled';

export const Wrapper = styled.div`
  display: flex;
  border-radius: 6px;
  padding: 3px 4px;
  max-width: 80px;
  box-sizing: border-box;

  &:hover {
    background: #414144;
  }
`;
